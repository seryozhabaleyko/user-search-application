const alertReducer = (state, { type, payload }) => {

    switch (type) {
        case 'SHOW_ALERT':
            return payload;
        case 'HIDE_ALERT':
            return null;
        default:
            return state;
    }

};

export default alertReducer;